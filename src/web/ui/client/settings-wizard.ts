const SETUP_WIZARDS={
  telegram:{
    id:'telegram',
    label:'Telegram',
    description:'Create a Telegram bot, choose polling or webhook delivery, and enable the Telegram adapter.',
    docs:[
      ['BotFather','https://t.me/BotFather'],
      ['Telegram Bot API','https://core.telegram.org/bots/api'],
      ['Webhook setup','https://core.telegram.org/bots/api#setwebhook']
    ],
    defaults:{TELEGRAM_ENABLED:'true',TELEGRAM_TRANSPORT:'polling',TELEGRAM_WEBHOOK_HOST:'127.0.0.1',TELEGRAM_WEBHOOK_PORT:'8080',TELEGRAM_WEBHOOK_PATH:'/telegram/webhook'},
    required:['TELEGRAM_BOT_TOKEN'],
    steps:[
      {title:'Create the bot',body:'Open BotFather, create a bot with /newbot, then paste only the token value into NordRelay. Do not paste the full BotFather message.',settings:['TELEGRAM_BOT_TOKEN'],links:[['Open BotFather','https://t.me/BotFather']]},
      {title:'Choose delivery mode',body:'Polling is the simplest setup and works without a public URL. Use webhook only when this NordRelay instance is reachable through HTTPS from Telegram.',settings:['TELEGRAM_TRANSPORT','TELEGRAM_WEBHOOK_URL','TELEGRAM_WEBHOOK_HOST','TELEGRAM_WEBHOOK_PORT','TELEGRAM_WEBHOOK_PATH','TELEGRAM_WEBHOOK_SECRET'],links:[['Webhook documentation','https://core.telegram.org/bots/api#setwebhook']]},
      {title:'Enable and secure access',body:'Enable Telegram only after the token is configured. Access is still controlled by NordRelay users, groups, and linked Telegram identities or registered chats.',settings:['TELEGRAM_ENABLED'],links:[['Bot API overview','https://core.telegram.org/bots/api']]}
    ]
  },
  discord:{
    id:'discord',
    label:'Discord',
    description:'Create a Discord application, configure slash commands or message commands, invite the bot, and enable Discord.',
    docs:[
      ['Discord Developer Portal','https://discord.com/developers/applications'],
      ['Privileged intents','https://discord.com/developers/docs/topics/gateway#privileged-intents'],
      ['OAuth2 bot invite','https://discord.com/developers/docs/topics/oauth2']
    ],
    defaults:{DISCORD_ENABLED:'true',DISCORD_MESSAGE_CONTENT_ENABLED:'true',DISCORD_COMMAND_MODE:'both',DISCORD_AUTO_REGISTER_COMMANDS:'true'},
    required:['DISCORD_BOT_TOKEN','DISCORD_CLIENT_ID'],
    steps:[
      {title:'Create application and bot',body:'Create an application in the Discord Developer Portal, add a bot, copy the bot token, and copy the Application ID from General Information.',settings:['DISCORD_BOT_TOKEN','DISCORD_CLIENT_ID'],links:[['Developer Portal','https://discord.com/developers/applications']]},
      {title:'Commands, intents, and invite',body:'For regular text commands, enable Message Content Intent under Bot > Privileged Gateway Intents. Invite the bot with bot and applications.commands scopes.',settings:['DISCORD_MESSAGE_CONTENT_ENABLED','DISCORD_COMMAND_MODE','DISCORD_AUTO_REGISTER_COMMANDS'],links:[['Privileged intents','https://discord.com/developers/docs/topics/gateway#privileged-intents'],['OAuth2 scopes','https://discord.com/developers/docs/topics/oauth2#shared-resources-oauth2-scopes']]},
      {title:'Limit server and channel scope',body:'Enable Discord Developer Mode, then right-click servers and channels to copy IDs. These allow-lists run before the NordRelay user/group access checks.',settings:['DISCORD_GUILD_IDS','DISCORD_ALLOWED_GUILD_IDS','DISCORD_ALLOWED_CHANNEL_IDS'],links:[['Where to find IDs','https://support.discord.com/hc/en-us/articles/206346498-Where-can-I-find-my-User-Server-Message-ID']]},
      {title:'Enable Discord',body:'Finish the Discord setup in this order before using NordRelay in a guild channel.',settings:['DISCORD_ENABLED'],links:[['Developer Portal','https://discord.com/developers/applications'],['OAuth2 URL Generator','https://discord.com/developers/applications']],checklist:['Open OAuth2 > URL Generator in the Discord Developer Portal for this application.','Select the scopes bot and applications.commands.','Grant bot permissions: View Channels, Send Messages, Send Messages in Threads, Read Message History, Attach Files, and Embed Links.','Open the generated invite URL and add the bot to the Discord server.','If message commands should become prompts, enable Message Content Intent under Bot > Privileged Gateway Intents and keep DISCORD_MESSAGE_CONTENT_ENABLED=true.','Save DISCORD_ENABLED=true and restart NordRelay.','Link your Discord account to a NordRelay user with /link <code>.','Run /register_channel in every guild channel or thread that should be allowed.','If DISCORD_ALLOWED_GUILD_IDS or DISCORD_ALLOWED_CHANNEL_IDS are set, make sure the server and channel IDs are included.']}
    ]
  },
  slack:{
    id:'slack',
    label:'Slack',
    description:'Create a Slack app, configure Socket Mode or HTTP Events, add slash commands, and enable Slack.',
    docs:[
      ['Slack API Apps','https://api.slack.com/apps'],
      ['Socket Mode','https://api.slack.com/apis/connections/socket'],
      ['Slash commands','https://api.slack.com/interactivity/slash-commands']
    ],
    defaults:{SLACK_ENABLED:'true',SLACK_SOCKET_MODE:'true',SLACK_PORT:'3000',SLACK_COMMAND:'/nordrelay'},
    required:['SLACK_BOT_TOKEN'],
    steps:[
      {title:'Create and install the Slack app',body:'Create a Slack app, add bot scopes, install it into the workspace, and copy the OAuth bot token that starts with xoxb-.',settings:['SLACK_BOT_TOKEN'],links:[['Slack API Apps','https://api.slack.com/apps'],['OAuth scopes','https://api.slack.com/authentication/oauth-v2']]},
      {title:'Choose transport mode',body:'Socket Mode is recommended because it avoids exposing a public HTTP endpoint. For Socket Mode, create an app-level token with connections:write. For HTTP Events, use the Signing Secret and configure an externally reachable URL.',settings:['SLACK_SOCKET_MODE','SLACK_APP_TOKEN','SLACK_SIGNING_SECRET','SLACK_PORT'],links:[['Socket Mode','https://api.slack.com/apis/connections/socket'],['Events API','https://api.slack.com/apis/events-api']]},
      {title:'Commands and scope',body:'Configure the slash command in Slack and optionally restrict workspaces or channels before NordRelay user/group permissions are applied.',settings:['SLACK_COMMAND','SLACK_ALLOWED_TEAM_IDS','SLACK_ALLOWED_CHANNEL_IDS','SLACK_MESSAGE_CONTENT_ENABLED'],links:[['Slash commands','https://api.slack.com/interactivity/slash-commands'],['Finding Slack IDs','https://slack.com/help/articles/221769328-Locate-your-Slack-URL-or-ID']]},
      {title:'Enable Slack',body:'Enable the adapter after tokens and command settings are ready. Users still need NordRelay permissions and linked Slack identities or registered channels.',settings:['SLACK_ENABLED'],links:[['Slack API Apps','https://api.slack.com/apps']]}
    ]
  },
  matrix:{
    id:'matrix',
    label:'Matrix',
    description:'Create a Matrix bot user, configure access-token sync, invite it to rooms, and enable Matrix.',
    docs:[
      ['Matrix client-server API','https://spec.matrix.org/latest/client-server-api/'],
      ['Matrix access tokens','https://spec.matrix.org/latest/client-server-api/#using-access-tokens'],
      ['Matrix room aliases and IDs','https://spec.matrix.org/latest/appendices/#room-ids-and-event-ids']
    ],
    defaults:{MATRIX_ENABLED:'true',MATRIX_AUTOJOIN_INVITES:'true',MATRIX_MESSAGE_CONTENT_ENABLED:'true',MATRIX_COMMAND_PREFIX:'!nr',MATRIX_SYNC_TIMEOUT_MS:'30000',MATRIX_POLL_TIMEOUT_MS:'35000'},
    required:['MATRIX_HOMESERVER_URL','MATRIX_ACCESS_TOKEN','MATRIX_USER_ID'],
    steps:[
      {title:'Create bot account',body:'Create or select a Matrix account dedicated to NordRelay, sign in with a Matrix client or homeserver tooling, and copy an access token for that bot account.',settings:['MATRIX_HOMESERVER_URL','MATRIX_ACCESS_TOKEN','MATRIX_USER_ID','MATRIX_DEVICE_ID'],links:[['Client-server API','https://spec.matrix.org/latest/client-server-api/']]},
      {title:'Choose room behavior',body:'Invite the bot user to each room. Auto-join accepts room invites while user/group permissions still decide whether a room can run prompts.',settings:['MATRIX_AUTOJOIN_INVITES','MATRIX_ALLOWED_ROOM_IDS'],links:[['Matrix room IDs','https://spec.matrix.org/latest/appendices/#room-ids-and-event-ids']]},
      {title:'Commands and message content',body:'Slash-style commands such as /session work in normal Matrix messages. The text prefix is useful when slash-style messages are intercepted by a client.',settings:['MATRIX_MESSAGE_CONTENT_ENABLED','MATRIX_COMMAND_PREFIX','MATRIX_SYNC_TIMEOUT_MS','MATRIX_POLL_TIMEOUT_MS'],links:[['Matrix sync API','https://spec.matrix.org/latest/client-server-api/#get_matrixclientv3sync']]},
      {title:'Enable Matrix',body:'Enable Matrix after homeserver, token, and user id are configured. Users still need NordRelay permissions and linked Matrix identities or registered rooms.',settings:['MATRIX_ENABLED'],links:[['Matrix access tokens','https://spec.matrix.org/latest/client-server-api/#using-access-tokens']],checklist:['Invite the bot user to every room that should use NordRelay.','Save MATRIX_ENABLED=true and restart NordRelay.','Create a Matrix link code from the Users page or with nordrelay user matrix-link-code.','Send /link <code> or !nr link <code> from the Matrix account to link it to a NordRelay user.','Run /register_channel or !nr register_channel in each allowed room.','Avoid end-to-end encrypted rooms unless a bridge or future decryption layer is configured.']}
    ]
  }
};

function settingByKey(key){return (state.settings||[]).find(s=>s.key===key)||null}
function wizardInputValue(key){const values:WebuiStringMap=state.settingsWizard?.values||{};if(Object.prototype.hasOwnProperty.call(values,key))return values[key];const s=settingByKey(key);if(!s)return'';if(s.options||s.kind==='boolean')return s.configured?(s.value||''):'';return s.configured?(s.value||''):(s.effectiveValue||'')}
function wizardEffectiveValue(key){const values:WebuiStringMap=state.settingsWizard?.values||{};const s=settingByKey(key);if(Object.prototype.hasOwnProperty.call(values,key)){const value=values[key];if((s?.options||s?.kind==='boolean')&&value==='')return s?.effectiveValue||'';return value}return s?(s.configured?(s.value||''):(s.effectiveValue||'')):''}
function wizardOriginalValue(key){const s=settingByKey(key);if(!s)return'';if(s.options||s.kind==='boolean')return s.configured?(s.value||''):'';return s.configured?(s.value||''):(s.effectiveValue||'')}
function isMaskedSettingValue(value){return /^\*+$/.test(String(value||''))||String(value||'').includes('...')}
function wizardKeys(wizard){return Array.from(new Set(wizard.steps.flatMap(step=>step.settings)))}
function safeDocLink(label,url){return '<a href="'+attr(url)+'" target="_blank" rel="noopener noreferrer">'+esc(label)+'</a>'}
function wizardLinkList(links=[]){return links.length?'<div class="wizard-links">'+links.map(([label,url])=>safeDocLink(label,url)).join('')+'</div>':''}
function wizardChecklist(items=[]){return items.length?'<ol class="wizard-checklist">'+items.map(item=>'<li>'+esc(item)+'</li>').join('')+'</ol>':''}
function startSettingsWizard(channel){const wizard=SETUP_WIZARDS[channel];if(!wizard)return;state.settingsWizard={channel,step:0,values:{},errors:[],testResult:null};renderSettingsWizardStep()}
function openSettingsWizardHome(){if(!can('settings.write')){toast('Permission required: settings.write');return}state.settingsWizard={home:true};renderSettingsWizardHome()}
function setSettingsChromeVisible(visible){document.getElementById('settingsTabHeader').style.display=visible?'':'none';document.getElementById('settingsSubnav').style.display=visible?'':'none';document.getElementById('settingsActions').style.display=visible?'':'none';document.getElementById('settingsLayout')?.classList.toggle('settings-wizard-layout',!visible)}
function closeSettingsWizard(){state.settingsWizard=null;setSettingsChromeVisible(true);renderSettings()}
function wizardRequiredValuePresent(key){const value=wizardEffectiveValue(key);if(!value)return false;const setting=settingByKey(key);if(isMaskedSettingValue(value))return setting?.kind==='secret'&&(setting.configured||setting.masked);return true}
function wizardMissingRequired(wizard){return (wizard.required||[]).filter(key=>!wizardRequiredValuePresent(key))}
function renderSettingsWizardHome(){setSettingsChromeVisible(false);const cards=Object.values(SETUP_WIZARDS).map(wizard=>{const missing=wizardMissingRequired(wizard);const docs=wizardLinkList(wizard.docs);return '<div class="wizard-card"><strong>'+esc(wizard.label)+' <span class="adapter-status '+(missing.length?'planned':'enabled')+'">'+esc(missing.length?missing.length+' missing':'ready')+'</span></strong><p>'+esc(wizard.description)+'</p><small>'+esc(missing.length?'Missing: '+missing.join(', '):'Required values are present or configured.')+'</small>'+docs+'<button type="button" data-start-wizard="'+attr(wizard.id)+'">Start wizard</button></div>'}).join('');document.getElementById('settingsForm').innerHTML='<div class="settings-wizard"><div class="wizard-header"><div><h2>Setup wizard</h2><p>Choose a chat adapter and follow the guided setup. Bot/app credentials only enable transport; NordRelay user and group permissions still control access.</p></div><button type="button" class="secondary" id="backToSettingsBtn">Back to settings</button></div><div class="wizard-choice-grid">'+cards+'</div></div>';document.getElementById('backToSettingsBtn').onclick=closeSettingsWizard;document.querySelectorAll('[data-start-wizard]').forEach(b=>b.onclick=()=>startSettingsWizard(b.dataset.startWizard));applyPermissions()}
function wizardSettingInput(s){const value=wizardInputValue(s.key);const attrs=' data-wizard-setting="'+attr(s.key)+'" data-original-value="'+attr(wizardOriginalValue(s.key))+'"';const blankLabel=s.effectiveValue?'Unset / inherit active default ('+s.effectiveValue+')':'Unset / inherit active default';if(s.options){return '<select'+attrs+'><option value="">'+esc(blankLabel)+'</option>'+s.options.map(o=>'<option value="'+attr(o)+'" '+(value===o?'selected':'')+'>'+esc(o)+'</option>').join('')+'</select>'}if(s.kind==='boolean'){return '<select'+attrs+'><option value="">'+esc(blankLabel)+'</option><option value="true" '+(value==='true'?'selected':'')+'>true</option><option value="false" '+(value==='false'?'selected':'')+'>false</option></select>'}if(s.kind==='json')return '<textarea rows="4"'+attrs+'>'+esc(value)+'</textarea>';return '<input'+attrs+' value="'+attr(value)+'" '+(s.kind==='secret'?'type="password" autocomplete="new-password"':'')+'>'}
function renderWizardSetting(key){const s=settingByKey(key);if(!s)return '<div class="setting"><strong>'+esc(key)+'</strong><small>Unknown setting.</small></div>';return '<div class="setting" data-wizard-setting-box="'+attr(key)+'" data-restart-required="'+(s.restartRequired?'true':'false')+'">'+settingLabel(s)+wizardSettingInput(s)+'<small>'+esc(s.key+' - '+s.description)+(s.effectiveValue?' Active: '+esc(s.effectiveValue)+'.':'')+(s.restartRequired?' Restart required.':'')+'</small><div class="setting-error"></div></div>'}
function renderWizardProgress(wizard){return '<div class="wizard-progress">'+wizard.steps.map((step,index)=>'<button type="button" data-wizard-step="'+index+'" class="'+(index===state.settingsWizard.step?'active':'')+'">'+esc(String(index+1)+'. '+step.title)+'</button>').join('')+'</div>'}
function renderSettingsWizardStep(){const wizard=SETUP_WIZARDS[state.settingsWizard?.channel];if(!wizard){renderSettingsWizardHome();return}const step=wizard.steps[state.settingsWizard.step]||wizard.steps[0];setSettingsChromeVisible(false);document.getElementById('settingsForm').innerHTML='<div class="settings-wizard"><div class="wizard-header"><div><h2>'+esc(wizard.label)+' setup wizard</h2><p>'+esc(wizard.description)+'</p></div><button type="button" class="secondary" id="wizardHomeBtn">Wizard home</button></div>'+renderWizardProgress(wizard)+'<div class="wizard-step"><h3>'+esc(step.title)+'</h3><p>'+esc(step.body)+'</p>'+wizardLinkList(step.links)+wizardChecklist(step.checklist)+'<div id="wizardRestartBanner"></div><div class="settings-grid">'+step.settings.map(renderWizardSetting).join('')+'</div><div id="wizardErrors" class="wizard-errors"></div><div id="wizardTestResult" class="wizard-test-result"></div><div class="wizard-actions"><button type="button" id="wizardPrevBtn" class="secondary">Back</button><button type="button" id="wizardNextBtn">'+esc(state.settingsWizard.step===wizard.steps.length-1?'Review':'Next')+'</button><button type="button" id="wizardTestBtn" class="secondary">Test setup</button><button type="button" id="wizardSaveBtn">Save wizard settings</button><button type="button" id="wizardSaveRestartBtn" class="secondary"'+disabledAttr('system.restart')+'>Save and restart</button></div><div class="setting-help">After transport is configured, link users and register allowed chats or channels in the Users page.</div></div></div>';bindWizardUx();renderWizardValidation()}
function bindWizardUx(){document.getElementById('wizardHomeBtn').onclick=renderSettingsWizardHome;document.querySelectorAll('[data-wizard-step]').forEach(b=>b.onclick=()=>{collectWizardValues();state.settingsWizard.step=Number(b.dataset.wizardStep)||0;renderSettingsWizardStep()});document.querySelectorAll('[data-wizard-setting]').forEach(el=>{el.oninput=()=>{state.settingsWizard.values[el.dataset.wizardSetting]=el.value;renderWizardValidation()};el.onchange=el.oninput});document.getElementById('wizardPrevBtn').onclick=()=>{collectWizardValues();if(state.settingsWizard.step>0){state.settingsWizard.step--;renderSettingsWizardStep()}else renderSettingsWizardHome()};document.getElementById('wizardNextBtn').onclick=()=>{collectWizardValues();if(state.settingsWizard.step<SETUP_WIZARDS[state.settingsWizard.channel].steps.length-1){state.settingsWizard.step++;renderSettingsWizardStep()}else renderWizardReview()};document.getElementById('wizardSaveBtn').onclick=()=>safe(()=>saveWizard(false));document.getElementById('wizardSaveRestartBtn').onclick=()=>safe(()=>saveWizard(true));document.getElementById('wizardTestBtn').onclick=()=>safe(testWizardSetup);applyPermissions()}
function collectWizardValues(){document.querySelectorAll('[data-wizard-setting]').forEach(el=>{state.settingsWizard.values[el.dataset.wizardSetting]=el.value})}
function collectWizardPatch(wizard){collectWizardValues();const patch:WebuiStringMap={};for(const rawKey of wizardKeys(wizard)){const key=String(rawKey);if(!Object.prototype.hasOwnProperty.call(state.settingsWizard.values,key))continue;const value=state.settingsWizard.values[key];if(value!==wizardOriginalValue(key))patch[key]=value}return patch}
function collectWizardSettings(wizard){collectWizardValues();const settings:WebuiStringMap={};for(const rawKey of wizardKeys(wizard)){const key=String(rawKey);settings[key]=wizardEffectiveValue(key)}return settings}
function parseList(text){return String(text||'').split(',').map(item=>item.trim()).filter(Boolean)}
function wizardValueBool(value){return ['true','1','yes','on'].includes(String(value||'').toLowerCase())}
function validateSnowflakeList(value,label,required=false){const items=parseList(value);if(required&&items.length===0)return [label+' is required.'];return items.filter(item=>!/^[0-9]{5,32}$/.test(item)).map(item=>label+' contains invalid Discord ID: '+item)}
function validateSlackIdList(value,label){return parseList(value).filter(item=>!/^[A-Z0-9]{2,64}$/.test(item)).map(item=>label+' contains invalid Slack ID: '+item)}
function validateMatrixRoomList(value,label){return parseList(value).filter(item=>!/^[!#][^:\s]+:[^:\s]+/.test(item)).map(item=>label+' contains invalid Matrix room id or alias: '+item)}
function validateWizard(wizard){const v=Object.fromEntries(wizardKeys(wizard).map(key=>[key,wizardEffectiveValue(key)]));const errors=[];const warnings=[];if(wizard.id==='telegram'){if(!v.TELEGRAM_BOT_TOKEN)errors.push('Telegram bot token is required.');else if(!isMaskedSettingValue(v.TELEGRAM_BOT_TOKEN)&&!/^[0-9]{5,}:[A-Za-z0-9_-]{20,}$/.test(v.TELEGRAM_BOT_TOKEN))errors.push('Telegram bot token does not look like a BotFather token.');if(!['polling','webhook'].includes(v.TELEGRAM_TRANSPORT||'polling'))errors.push('Telegram transport must be polling or webhook.');if((v.TELEGRAM_TRANSPORT||'polling')==='webhook'){if(!String(v.TELEGRAM_WEBHOOK_URL||'').startsWith('https://'))errors.push('Webhook public URL must be HTTPS.');if(!v.TELEGRAM_WEBHOOK_HOST)errors.push('Webhook bind host is required.');if(!Number.isFinite(Number(v.TELEGRAM_WEBHOOK_PORT)))errors.push('Webhook port must be numeric.');if(!String(v.TELEGRAM_WEBHOOK_PATH||'').startsWith('/'))errors.push('Webhook path must start with /.')}}if(wizard.id==='discord'){if(!v.DISCORD_BOT_TOKEN)errors.push('Discord bot token is required.');else if(!isMaskedSettingValue(v.DISCORD_BOT_TOKEN)&&String(v.DISCORD_BOT_TOKEN).length<20)errors.push('Discord bot token is too short.');if(!v.DISCORD_CLIENT_ID)errors.push('Discord client ID is required.');else errors.push(...validateSnowflakeList(v.DISCORD_CLIENT_ID,'Discord client ID',true));errors.push(...validateSnowflakeList(v.DISCORD_GUILD_IDS,'Discord guild IDs'));errors.push(...validateSnowflakeList(v.DISCORD_ALLOWED_GUILD_IDS,'Allowed Discord guilds'));errors.push(...validateSnowflakeList(v.DISCORD_ALLOWED_CHANNEL_IDS,'Allowed Discord channels'));if(['message','both'].includes(v.DISCORD_COMMAND_MODE||'both')&&!wizardValueBool(v.DISCORD_MESSAGE_CONTENT_ENABLED))warnings.push('Message command mode needs Message Content Intent enabled in the Discord Developer Portal.')}if(wizard.id==='slack'){if(!v.SLACK_BOT_TOKEN)errors.push('Slack bot token is required.');else if(!isMaskedSettingValue(v.SLACK_BOT_TOKEN)&&!String(v.SLACK_BOT_TOKEN).startsWith('xoxb-'))errors.push('Slack bot token should start with xoxb-.');if(wizardValueBool(v.SLACK_SOCKET_MODE)){if(!v.SLACK_APP_TOKEN)errors.push('Slack app token is required for Socket Mode.');else if(!isMaskedSettingValue(v.SLACK_APP_TOKEN)&&!String(v.SLACK_APP_TOKEN).startsWith('xapp-'))errors.push('Slack app token should start with xapp-.')}else{if(!v.SLACK_SIGNING_SECRET)errors.push('Slack signing secret is required when Socket Mode is disabled.');if(!Number.isFinite(Number(v.SLACK_PORT)))errors.push('Slack HTTP port must be numeric.')}if(v.SLACK_COMMAND&&!String(v.SLACK_COMMAND).startsWith('/'))errors.push('Slack slash command must start with /.');errors.push(...validateSlackIdList(v.SLACK_ALLOWED_TEAM_IDS,'Allowed Slack teams'));errors.push(...validateSlackIdList(v.SLACK_ALLOWED_CHANNEL_IDS,'Allowed Slack channels'))}if(wizard.id==='matrix'){if(!v.MATRIX_HOMESERVER_URL)errors.push('Matrix homeserver URL is required.');else if(!/^https?:\/\//.test(String(v.MATRIX_HOMESERVER_URL)))errors.push('Matrix homeserver URL must start with http:// or https://.');if(!v.MATRIX_ACCESS_TOKEN)errors.push('Matrix access token is required.');else if(!isMaskedSettingValue(v.MATRIX_ACCESS_TOKEN)&&String(v.MATRIX_ACCESS_TOKEN).length<12)errors.push('Matrix access token is too short.');if(!v.MATRIX_USER_ID)errors.push('Matrix bot user ID is required.');else if(!/^@[^:\s]+:[^:\s]+/.test(String(v.MATRIX_USER_ID)))errors.push('Matrix bot user ID must look like @user:server.');if(!String(v.MATRIX_COMMAND_PREFIX||'!nr').trim())errors.push('Matrix command prefix is required.');if(!Number.isFinite(Number(v.MATRIX_SYNC_TIMEOUT_MS)))errors.push('Matrix sync timeout must be numeric.');if(!Number.isFinite(Number(v.MATRIX_POLL_TIMEOUT_MS)))errors.push('Matrix poll timeout must be numeric.');if(Number(v.MATRIX_POLL_TIMEOUT_MS||0)<Number(v.MATRIX_SYNC_TIMEOUT_MS||0))warnings.push('Matrix poll timeout should be at least the sync timeout.');errors.push(...validateMatrixRoomList(v.MATRIX_ALLOWED_ROOM_IDS,'Allowed Matrix rooms'));warnings.push('End-to-end encrypted Matrix rooms are not decrypted by NordRelay. Use an unencrypted bot room unless you run a bridge.')}return{errors,warnings}}
function renderWizardValidation(){const wizard=SETUP_WIZARDS[state.settingsWizard?.channel];if(!wizard)return;const result=validateWizard(wizard);const target=document.getElementById('wizardErrors');if(target)target.innerHTML=[...result.errors.map(e=>'<div class="wizard-error">'+esc(e)+'</div>'),...result.warnings.map(w=>'<div class="wizard-warning">'+esc(w)+'</div>')].join('');const changed=Object.keys(collectWizardPatch(wizard)).length;const banner=document.getElementById('wizardRestartBanner');if(banner)banner.innerHTML=changed?'<div class="restart-banner">'+changed+' wizard setting(s) will be saved. A NordRelay restart may be required.</div>':'';document.getElementById('wizardSaveBtn').disabled=!can('settings.write')||result.errors.length>0;document.getElementById('wizardSaveRestartBtn').disabled=!can('settings.write')||!can('system.restart')||result.errors.length>0}
function renderWizardReview(){const wizard=SETUP_WIZARDS[state.settingsWizard.channel];collectWizardValues();const validation=validateWizard(wizard);const patch=collectWizardPatch(wizard);document.getElementById('settingsForm').innerHTML='<div class="settings-wizard"><div class="wizard-header"><div><h2>'+esc(wizard.label)+' review</h2><p>Review changed settings before saving. Secret values stay masked when they are already configured.</p></div><button type="button" class="secondary" id="wizardEditBtn">Back to wizard</button></div><div class="list">'+(Object.keys(patch).map(key=>'<div class="item"><strong>'+esc(key)+'</strong><small>'+esc(isMaskedSettingValue(patch[key])?'configured secret / unchanged':patch[key]||'(unset)')+'</small></div>').join('')||'<div class="item">No wizard changes.</div>')+'</div><div id="wizardErrors">'+[...validation.errors.map(e=>'<div class="wizard-error">'+esc(e)+'</div>'),...validation.warnings.map(w=>'<div class="wizard-warning">'+esc(w)+'</div>')].join('')+'</div><div class="wizard-actions"><button type="button" id="wizardSaveBtn">Save wizard settings</button><button type="button" id="wizardSaveRestartBtn" class="secondary"'+disabledAttr('system.restart')+'>Save and restart</button><button type="button" class="secondary" id="wizardHomeBtn">Wizard home</button></div></div>';document.getElementById('wizardEditBtn').onclick=renderSettingsWizardStep;document.getElementById('wizardHomeBtn').onclick=renderSettingsWizardHome;document.getElementById('wizardSaveBtn').onclick=()=>safe(()=>saveWizard(false));document.getElementById('wizardSaveRestartBtn').onclick=()=>safe(()=>saveWizard(true));document.getElementById('wizardSaveBtn').disabled=validation.errors.length>0;document.getElementById('wizardSaveRestartBtn').disabled=!can('system.restart')||validation.errors.length>0;applyPermissions()}
async function saveWizard(restart){const wizard=SETUP_WIZARDS[state.settingsWizard.channel];const validation=validateWizard(wizard);if(validation.errors.length){renderWizardValidation();toast('Wizard settings need attention');return}const patch=collectWizardPatch(wizard);const r=await api('/api/settings',{method:'PATCH',body:JSON.stringify({settings:patch})});if(r.errors&&r.errors.length){document.getElementById('settingsStatus').textContent='Fix '+r.errors.length+' setting error(s)';toast('Settings need attention');return}toast('Wizard settings saved'+(r.restartRequired?' - restart required':''));document.getElementById('settingsStatus').textContent=(r.changedKeys?.length?'Saved '+r.changedKeys.length+' wizard setting(s)':'No changes')+(r.restartRequired?' - restart required':'');if(restart){await api('/api/runtime/restart',{method:'POST'});toast('Settings saved and restart requested',{duration:6000})}await loadSettings()}
async function testWizardSetup(){const wizard=SETUP_WIZARDS[state.settingsWizard.channel];const validation=validateWizard(wizard);if(validation.errors.length){renderWizardValidation();toast('Fix validation errors before testing');return}const target=document.getElementById('wizardTestResult');target.innerHTML=loadingHtml('Testing setup...');const result=await api('/api/settings/wizard/test',{method:'POST',body:JSON.stringify({channel:wizard.id,settings:collectWizardSettings(wizard)})});target.innerHTML=(result.checks||[]).map(check=>'<div class="item"><strong>'+esc(check.label)+' <span class="adapter-status '+(check.status==='ok'?'enabled':check.status==='warn'?'planned':'disabled')+'">'+esc(check.status)+'</span></strong><small>'+esc(check.detail||'')+'</small></div>').join('')||'<div class="item">No checks returned.</div>'}
document.getElementById('settingsWizardBtn').onclick=openSettingsWizardHome;
